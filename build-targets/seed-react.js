/**
 * JS-Seed React main module
 * @module react/main
 */

(function(win) {
  var seed = win.seed = win.seed || {},
      react = seed.react = {};

  react.Button = require('src/react/button');
})(window);
